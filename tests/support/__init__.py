"""
The shared engine: the ported comparison / tolerance / frame / strategy layer and the new declarative core
(declaration, registry, synthesis, messages, rungs). Each module is imported by its own path; this package marker
carries no re-export surface of its own.
"""
